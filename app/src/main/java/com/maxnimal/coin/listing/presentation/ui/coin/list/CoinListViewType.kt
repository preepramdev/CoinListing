package com.maxnimal.coin.listing.presentation.ui.coin.list

enum class CoinListViewType(val value: Int) {
    TOP_RANK(111),
    OTHERS(222),
    HEADER_TOP_RANK(333),
    HEADER_BUY_SELL_HOLD(444);
}